import FormComponent from "../components/FormComponent.tsx";

const CreateCarPage = () => {
    return (
        <div>
            <FormComponent/>
        </div>
    );
};

export default CreateCarPage;
